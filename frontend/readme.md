- `cargo install wasm-pack`: used in `wasm-pack build --target web`
- `brew install wabt`: used in `wasm2wat rust_interface_bg.wasm > rust_interface_bg.wat`
    check the [wabt](https://github.com/WebAssembly/wabt) document